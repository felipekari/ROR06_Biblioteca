<%= form_with(model: book, local: true) do |form| %>
  <% if book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
      <% book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title, "Titulo" %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :author, "Autor" %>
    <%= form.text_field :author %>
  </div>

  <div class="field">
    <%= form.label :status, "Estado" %>
    <%= form.select :status, Book.statuses.keys %>
  </div>

  <div class="field">
    <%= form.label :borrowing_date, "Fecha de prestamo" %>
    <%= form.date_field :borrowing_date %>
  </div>

  <div class="field">
    <%= form.label :givenback_date, "Fecha de devolucion" %>
    <%= form.date_field :givenback_date %>
  </div>

  <div class="field">
    <%= form.label :holder, "Poseedor" %>
    <%= form.text_field :holder %>
  </div>

  <div class="actions">
    <%= form.submit "Enviar" %>
  </div>
<% end %>
